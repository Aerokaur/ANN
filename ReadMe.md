# This library includes the Fortran-C interface for using ANN with FORTRAN libraries.
# To put FORTRAN modules in correct places, do `make install`. 
# Just add path to `lib` and `include` dircetories to use ANN with your code. 
